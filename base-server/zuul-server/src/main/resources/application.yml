server:
  port: 8763

spring:
  application:
    name: zuul-server
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
zuul:
  routes:
      path: /admin/**
      serviceId: admin-server

